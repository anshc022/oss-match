## Summary

<!-- What does this change, in one or two sentences? Write it for someone who
     has not read the issue. -->

## Linked issue

<!-- "Closes #12" links the issue and closes it when this merges.
     Use "Refs #12" if it only moves the issue forward. -->

Closes #

## Type of change

<!-- Tick everything that applies. -->

- [ ] Bug fix (does not change existing behaviour on purpose)
- [ ] New feature
- [ ] Documentation
- [ ] Refactor or tooling (no user-visible change)
- [ ] Accessibility fix
- [ ] Breaking change (needs a note in the summary above)

## Testing done

<!-- How did you check this works? Commands you ran, pages you clicked through,
     the case you tried that used to fail. "Ran the suite" alone is not enough
     for a behaviour change. -->

## Screenshots

<!-- Required for anything that changes the UI. Before and after if you can,
     and both light and dark mode. Drag images straight into this box. -->

## Checklist

- [ ] `npm test` passes
- [ ] `npm run lint` is clean
- [ ] `npm run typecheck` is clean
- [ ] `npm run build` succeeds
- [ ] I checked the preview deployment on this pull request
- [ ] No secrets, tokens, or connection strings are committed (`.env.local` stays local)
- [ ] I added or updated tests where the change is testable
